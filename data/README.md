# Data

Data files used by SmetaPro.

- `index.json` — available data profiles.
- `profiles/` — profile datasets.
- `schema/` — dataset schemas.

The previous `getto-dev/check-data` repository remains unchanged during the migration.
